.container
  - flash.each do |name, msg|
    - if msg.is_a?(String)
      %div{:class => "alert alert-#{name.to_s == 'notice' ? 'success' : 'alert'} alert-dismissable", "data-alert" => ""}
        = content_tag :div, msg
