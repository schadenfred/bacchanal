var $comment = $('#comment-<%= @comment.parent.id %>').children(".comment-content");

$comment.find('.comment-actions').hide();
$comment.find('.comment-reply-cancel').show();
$comment.find('.comment-reply').append("<%= j render "comments/form"%>");

