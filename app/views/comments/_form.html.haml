= simple_form_for(@comment) do |f|
  = f.error_notification

  .form-inputs
    = f.input :content
    = f.input :commentable_id
    = f.input :commentable_type

  .form-actions
    = f.button :submit
