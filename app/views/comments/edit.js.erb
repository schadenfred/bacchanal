var $comment = $('#comment-<%= @comment.id %>');

$comment.find('.comment-actions').hide();
$comment.find('.comment-reply-cancel').show();
$comment.find('.comment-reply').after("<%= j render "comments/form"%>");



